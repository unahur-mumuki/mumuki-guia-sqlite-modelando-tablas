type: display
seed: |
  CREATE TABLE Infraccion (
    id int(10) unsigned NOT NULL AUTO_INCREMENT PRIMARY KEY,
    cantidadPuntosDescontados tinyint(3) unsigned DEFAULT NULL,
    monto decimal(8,2) unsigned NOT NULL,
    observacion varchar(255) DEFAULT NULL
  );
  INSERT INTO Infraccion VALUES (null, 0, 1500, "Estacionamiento en lugar no permitido"), (null, 5, 3500, "Exceso de velocidad por encima de los 30 km/h");
query: SELECT * FROM Infraccion;